# Legacy Workbench Archive

This directory stores the full pre-slim `LegacyWorkbench.vue` source outside `src/`.
It is not routed, not built, and not a place for new feature work.

The active legacy route is now `src/workspaces/legacy/LegacyWorkbench.vue`, which is only a migration hub.
